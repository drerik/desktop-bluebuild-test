---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: bazzite-asus-rog-flow-z13
# description will be included in the image's metadata
description: Bazzite deployment with extra tools for Asus ROG Flow Z13

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/bazzite-gnome
image-version: stable-43 # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: rpm-ostree
    repos: []
      # - https://copr.fedorainfracloud.org/coprs/atim/starship/repo/fedora-%OS_VERSION%/atim-starship-fedora-%OS_VERSION%.repo
    install: []
      # sudo sed 's/#firewall_backend.*$/firewall_backend = \"iptables\"/' /etc/libvirt/network.conf
    remove: []
      # example: removing firefox (in favor of the flatpak)
      # "firefox" is the main package, "firefox-langpacks" is a dependency
  - type: default-flatpaks
    configurations:
      - scope: user
        repo:
          # you can override the title to make inclusion of both system and user repos clearer
          title: Flathub (user)
          install: []
      - notify: true
        scope: system
        repo:
          title: Flathub (system)      
          install: []
  - type: rl-theme
    source: local
  - type: netbird
    source: local
  - type: signing # this sets up the proper policy & signing files for signed images to work fully

